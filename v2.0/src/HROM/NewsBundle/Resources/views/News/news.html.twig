<div class="news">
    <h1>{{ news.title }}</h1>

    {% if not (news.picture is empty) %}<img src="{{ asset(news.picture.webPath) }}" alt="{{ news.picture.alt }}" />{% endif %}

    <p>{{ news.content }}</p><br />

    <p class="signature">Ecrit par {{ news.author.surname }} {{ news.author.givenName }} le {{ news.instant|date("d/m/Y à H:i") }}</p>
</div>